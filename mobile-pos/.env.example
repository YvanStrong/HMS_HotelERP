# Local dev / Expo Go — LAN HTTP backend on hotel WiFi
EXPO_PUBLIC_API_URL=http://172.20.10.2:8080
EXPO_PUBLIC_TAX_RATE=0.18
EXPO_PUBLIC_ALLOW_CLEARTEXT=true

# After `eas init`, copy project UUID from expo.dev → Credentials
# Required for push on dev builds (not Expo Go).
# EXPO_PUBLIC_EAS_PROJECT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
